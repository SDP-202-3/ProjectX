#include <iostream>
#include <string>
#include <ctime>
#include <fstream>
#include <vector>
#include <Windows.h>
#include <conio.h>

using namespace std;

class Car {
	int brand;
	int model;
	int year;
	int state;
	Car(int x, int y, int z)

		
    void print();
	void service();
	void cost();

};

Car:Car(int x, int y, int z) {
cost = cost;
model = model;
year = year;
}

void Car::print() {
	cout << brand << model << state << endl;
}

void Car::cost() {
	this->car_dealership_price + procent;

}